<h1>プロフィール編集画面</h1>
<%= form_for(current_user) do |f| %>
    <%= f.label :"ユーザ名" %>
    <%= f.text_field :name, class: 'form-control' %>  
    <%= f.text_area placeholder: "Compose new micropost..." %>
    <%= f.submit "更新", class: "btn btn-primary" %>
<% end %>
