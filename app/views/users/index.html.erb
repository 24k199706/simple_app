<table class="table table-hover">
    <tbody>
        <% @user.each do |user| %>
            <tr>
                <td><%= user.name %> | <%= link_to "delete", resign_path(user), method: :delete %></td>
            </tr>
        <% end %>
    </tbody>
</table>