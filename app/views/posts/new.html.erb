<H1>新規投稿画面</H1>
<main class="container">
    <div class="starter-template text-center py-5 px-3">
        <%= form_for(@post) do |f| %>
            <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">タイトル</label>
            <%= f.text_area :title, :placeholder => "タイトルを入れてね" , class: "form-control" %><br>
            </div>
            <div class="mb-3">
            <label for="exampleFormControlInput1" class="form-label">本文</label>
            <textarea placeholder="ここに何か入力してね" class="form-control" name="post[content]" id="exampleFormControlTextarea1" rows="20"></textarea>
            </div>
            <%= f.submit "新規投稿", class: "btn btn-primary" %>
        <% end %>
    </div>
</main>
